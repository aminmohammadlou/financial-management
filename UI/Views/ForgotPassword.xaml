<Window x:Class="UI.Views.ForgotPassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="720" Width="1280"
        WindowStyle="None"
        Background="Transparent"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseDown="ForgotPassword_OnMouseDown"
        FontFamily="{StaticResource MainFont}">

    <Grid>
        <Border>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"  Style="{StaticResource LeftBackGround}"></Border>
                <Border Grid.Column="1"  Style="{StaticResource RightBackGround}"></Border>
            </Grid>

        </Border>

        <Border Style="{StaticResource OpacityCircle}"/>

        <Border Style="{StaticResource MainCircle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1.5*"></RowDefinition>
                    <RowDefinition Height="1.5*"></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="1.5*"></RowDefinition>
                    <RowDefinition Height="1.5*"></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="1.5*"></RowDefinition>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="1" Style="{StaticResource TitleText}">فراموشی رمز عبور</TextBlock>
                <TextBlock Grid.Row="2" Style="{StaticResource TitleText2}">ایمیل خود را وارد کنید</TextBlock>
                <Border Grid.Row="3" Style="{StaticResource RedLine}" />

                <TextBlock Grid.Row="4" Style="{StaticResource UserNameText}">:ایمیل</TextBlock>
                <Border Grid.Row="5" Style="{StaticResource InputBorder}">
                    <TextBox Style="{StaticResource InputTextBox}"></TextBox>
                </Border>

                <Button Grid.Row="6" Style="{StaticResource SignIn}">
                    دریافت کد تایید
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="25"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <TextBlock Name="BackText" Grid.Row="7" Style="{StaticResource SignUpText}">بازگشت</TextBlock>

            </Grid>

        </Border>

    </Grid>

</Window>
